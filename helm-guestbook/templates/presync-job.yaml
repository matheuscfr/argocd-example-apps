apiVersion: batch/v1
kind: Job
metadata:
  name: migration-job
spec:
  template:
    spec:
      containers:
      - name: migration-job
        image: busybox:stable
        command: ["/bin/sh"]
        args: ["-c", "echo 'Running database migrations...' sleep 10 echo 'Migrated'"]
      restartPolicy: Never
  backoffLimit: 4
